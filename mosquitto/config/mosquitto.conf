# Local dev broker config.
# Anonymous access is fine here because this only runs on your own machine
# during development. AWS IoT Core (used later for the real cloud backend)
# enforces TLS + certificate auth, so this is not a security concern for
# the graded deployment.

listener 1883
allow_anonymous true

persistence false
log_dest stdout
